using System.Collections.Generic;

namespace SourcepawnCondenser.SourcemodDefinition
{
    public class SMVariable : SMBaseDefinition
    {
        public SMVariable(int index, int length, string file, string name, string commentString) : base(index, length, file, name, commentString)
        {
        }
    }
}