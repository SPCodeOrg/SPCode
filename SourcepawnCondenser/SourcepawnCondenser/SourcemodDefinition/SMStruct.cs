namespace SourcepawnCondenser.SourcemodDefinition
{
    public class SMStruct : SMBaseDefinition
    {
        public SMStruct(int index, int length, string file, string name, string commentString) : base(index, length, file, name, commentString)
        {
        }
    }
}
